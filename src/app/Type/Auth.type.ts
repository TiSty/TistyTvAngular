export type Auth = {
    idLingua?: number,
    token: string | null,
    nome: string | null,
    idRuolo: number | null,
    idStato: number | null,
    potere: number []|null,
    idUtente: number|null,

}