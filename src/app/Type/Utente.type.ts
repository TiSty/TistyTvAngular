export type Utente = {
    nome: string | null,
    cognome: string | null,
    dataNascita: string | null,
    sesso: number | null,
    residenza: string | null,
    domicilio: string | null,
    ragioneSociale:number|null,
    cittadinanza:string|null,
}